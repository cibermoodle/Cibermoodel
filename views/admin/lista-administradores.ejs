<div class="container py-5">
  <div class="d-flex flex-wrap align-items-center justify-content-between gap-2 mb-4">
    <div>
      <h2 class="fw-bold mb-1">Todos los administradores</h2>
      <p class="text-muted mb-0">Listado completo de administradores registrados.</p>
    </div>
    <div class="d-flex gap-2">
      <a href="/admin" class="btn btn-outline-secondary">Volver al panel</a>
      <a href="/admin/crear-administrador" class="btn text-white" style="background:#1d4ed8;">Crear administrador</a>
    </div>
  </div>

  <form method="GET" action="/admin/administradores" class="card border-0 shadow-sm mb-3">
    <div class="card-body d-flex flex-wrap gap-2 align-items-center">
      <input
        type="text"
        name="q"
        class="form-control"
        style="max-width: 380px;"
        placeholder="Buscar por nombre, apellidos o email"
        value="<%= q || '' %>"
      >
      <button type="submit" class="btn text-white" style="background:#1d4ed8;">Buscar</button>
      <a href="/admin/administradores" class="btn btn-outline-secondary">Limpiar</a>
    </div>
  </form>

  <div class="card border-0 shadow-sm">
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-light">
          <tr>
            <th>ID Administrador</th>
            <th>Nombre</th>
            <th>Apellidos</th>
            <th>Email</th>
            <th class="text-end">Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% if (!administradores || administradores.length === 0) { %>
            <tr>
              <td colspan="5" class="text-center text-muted py-4">No hay administradores registrados.</td>
            </tr>
          <% } else { %>
            <% administradores.forEach(administrador => { %>
              <tr>
                <td><%= administrador.usuario_id %></td>
                <td><%= administrador.nombre %></td>
                <td><%= administrador.apellidos %></td>
                <td><%= administrador.email %></td>
                <td class="text-end">
                  <div class="d-inline-flex gap-2">
                    <a href="/admin/administradores/<%= administrador.usuario_id %>/editar" class="btn btn-sm btn-outline-warning">Editar</a>
                    <form action="/admin/administradores/<%= administrador.usuario_id %>/eliminar" method="POST" onsubmit="return confirm('¿Eliminar administrador?');" class="d-inline">
                      <button type="submit" class="btn btn-sm btn-outline-danger">Eliminar</button>
                    </form>
                  </div>
                </td>
              </tr>
            <% }) %>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>